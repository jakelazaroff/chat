define(function (require) {

  var React = require('react');
  var Canvas = require('components/canvas/canvas');

  var Application = React.createClass({
    render () {
      return (
        <div className="application">
        </div>
      );
    }
  });

  return Application;

});
